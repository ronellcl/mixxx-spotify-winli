# Core (all platforms): MIDI bridge between Mixxx and this script.
python-rtmidi>=1.5

# Platform-specific backends are resolved at runtime; the base requirements
# are enough for macOS (AppleScript), Windows (libspotifyctl) needs:
#   python -m pip install libspotifyctl        (Windows)
# Linux (dbus-python) needs:
#   python -m pip install dbus-python          (or system python3-dbus)
